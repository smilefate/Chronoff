<Window x:Class="Chronoff.ParolaPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chronoff"
        mc:Ignorable="d"
        Title="Chronoff - Parola Onay Ekranı" Height="320" Width="440"
        WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        Background="Transparent" Topmost="True">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundDark" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SecondaryContainer" Color="#60CDFF"/>
        <SolidColorBrush x:Key="TextLight" Color="#F1F1F1"/>
        <SolidColorBrush x:Key="TextMuted" Color="#A0A6AE"/>
        <SolidColorBrush x:Key="BorderColor" Color="#22FFFFFF"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#FF4D4D"/>

        <SolidColorBrush x:Key="PrimaryBlue" Color="#0078D4"/>
        
        <Style x:Key="FluentPasswordInput" TargetType="Control">
            <Setter Property="Background" Value="#11FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundDark}" CornerRadius="16" BorderThickness="1" BorderBrush="{StaticResource BorderColor}">
        <Border.RenderTransform>
            <TranslateTransform x:Name="shakeTransform" X="0"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Color="Black" Opacity="0.5" ShadowDepth="0"/>
        </Border.Effect>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="8" Background="#1160CDFF" Margin="0,0,16,0">
                    <TextBlock Text="🛡️" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="lblHeader" Text="Parola Doğrulaması Gerekli" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="lblSubheader" Text="Devam etmek için lütfen yönetici parolasını girin." FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Content Body Section -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,10">
                <TextBlock x:Name="lblPasswordLabel" Text="YÖNETİCİ PAROLASI" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,8"/>
                
                <!-- Password Inputs container with Show/Hide toggle -->
                <Grid>
                    <Border Background="#11FFFFFF" CornerRadius="8" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" Height="45">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="🔒" FontSize="14" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="12,0,4,0"/>
                            
                            <!-- PasswordBox (secure) -->
                            <PasswordBox x:Name="txtParola" Grid.Column="1" Background="Transparent" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0" CaretBrush="White" PasswordChanged="TxtPassword_PasswordChanged"/>
                            
                            <!-- TextBox (plain text - visible when checked) -->
                            <TextBox x:Name="txtParolaGoster" Grid.Column="1" Background="Transparent" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0" CaretBrush="White" Visibility="Collapsed" TextChanged="TxtPasswordVisible_TextChanged"/>
                            
                            <!-- Show / Hide toggle button -->
                            <ToggleButton x:Name="btnToggleShow" Grid.Column="2" Width="36" Height="36" Background="Transparent" BorderThickness="0" Margin="4,0" Cursor="Hand" Click="BtnToggleShow_Click">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <TextBlock x:Name="txtSimge" Text="👁️" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="txtSimge" Property="Text" Value="🙈"/>
                                                <Setter TargetName="txtSimge" Property="Opacity" Value="0.8"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="txtSimge" Property="Opacity" Value="1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Error Message -->
                <StackPanel x:Name="panelError" Orientation="Horizontal" Margin="0,6,0,0" Visibility="Collapsed">
                    <TextBlock Text="⚠️" FontSize="12" Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock x:Name="lblErrorText" Text="Geçersiz parola. Lütfen tekrar deneyin." FontSize="11" Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Info Box -->
                <Border Background="#0B60CDFF" BorderThickness="1" BorderBrush="#1F60CDFF" CornerRadius="8" Padding="12,10" Margin="0,15,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="ℹ️" FontSize="12" Foreground="{StaticResource SecondaryContainer}" Margin="0,1,10,0" VerticalAlignment="Top"/>
                        <TextBlock x:Name="lblInfoText" Grid.Column="1" Text="Bu işlem zamanlanmış eylemin iptal edilmesini içerir. Doğrulama, zamanlama bütünlüğünü korur." FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap"/>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- Footer Action Buttons -->
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="btnCancel" Content="İptal" Click="Cancel_Click" Margin="0,0,6,0" Height="40" Background="#11FFFFFF" Foreground="White" BorderThickness="1" BorderBrush="{StaticResource BorderColor}" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Grid.Column="1" x:Name="btnVerify" Content="Doğrula ve Devam Et" Click="Verify_Click" Margin="6,0,0,0" Height="40" Background="{StaticResource PrimaryBlue}" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>